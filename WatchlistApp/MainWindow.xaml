<Window x:Class="WatchlistApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WatchlistApp"
        mc:Ignorable="d"
        ResizeMode="CanResize"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="ImageButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition Width="6*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="9*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Content="Neue Watchlist" Grid.Row="0" Grid.Column="0" Click="AddWatchlist"/>

        <StackPanel Orientation="Horizontal" Grid.Column="1">
            <Label Content="Filter"></Label>
            <Label Content="Releasing"></Label>
            <ComboBox>
                <ComboBoxItem Content="Ja"></ComboBoxItem>
                <ComboBoxItem Content="Nein"></ComboBoxItem>
            </ComboBox>
            <TextBox x:Name="search_text_box" Width="200" TextChanged="SearchShow"/>
        </StackPanel>

        <ListBox x:Name="watchlist_listbox" ItemsSource="{Binding watchlists}" Grid.Column="0" Grid.Row="1" SelectionChanged="WatchlistSelectionChanged">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Button Style="{StaticResource ImageButtonStyle}" VerticalAlignment="Center" Click="DeleteWatchlist">
                            <Image Source="../resources/trashbin_icon.png" Width="16" Height="16"/>
                        </Button>
                        <TextBlock Text="{Binding Name}" FontSize="16" Margin="5" VerticalAlignment="Center" />
                        <Button Style="{StaticResource ImageButtonStyle}" VerticalAlignment="Center" HorizontalAlignment="Stretch" Click="AddShowToWatchlist">
                            <Image Source="../resources/add_icon.png" Width="16" Height="16"/>
                        </Button>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <ListBox x:Name="shows_listbox" ItemsSource="{Binding shows}" Grid.Column="1" Grid.Row="1">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="2" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Image Source="{Binding Image}" Stretch="UniformToFill" Height="150" Grid.Row="0" Grid.Column="0" />
                        <TextBlock Text="{Binding Name}" FontSize="20" FontWeight="Bold" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Description}" FontSize="14" TextWrapping="Wrap" Grid.Row="1" Grid.Column="1" VerticalAlignment="Top" />
                        <TextBlock Text="{Binding ReleaseDate, StringFormat='Release Date: {0:yyyy-MM-dd}'}" FontSize="12" Grid.Row="2" Grid.Column="1" VerticalAlignment="Bottom" />
                        <Grid Grid.Row="3" Grid.Column="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"></ColumnDefinition>
                                <ColumnDefinition Width="auto"></ColumnDefinition>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <StackPanel Orientation="Horizontal" Grid.Column="0">
                                <TextBlock Text="Wird noch veröffentlicht" FontSize="12" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <CheckBox VerticalAlignment="Center" Checked="IsReleasingChecked" Unchecked="IsReleasingUnchecked" >
                                    <CheckBox.Style>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="IsChecked" Value="False" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsReleasing}" Value="1">
                                                    <Setter Property="IsChecked" Value="True" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsReleasing}" Value="0">
                                                    <Setter Property="IsChecked" Value="False" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </CheckBox.Style>
                                </CheckBox>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" Grid.Column="1">
                                <TextBlock Text="Bereits angeschaut" FontSize="12" VerticalAlignment="Center" Margin="0,0,5,0"/>
                                <CheckBox VerticalAlignment="Center" Checked="AlreadyWatchedChecked" Unchecked="AlreadyWatchedUnchecked" >
                                    <CheckBox.Style>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="IsChecked" Value="False" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding AlreadyWatched}" Value="1">
                                                    <Setter Property="IsChecked" Value="True" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding AlreadyWatched}" Value="0">
                                                    <Setter Property="IsChecked" Value="False" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </CheckBox.Style>
                                </CheckBox>
                            </StackPanel>
                            <Button Style="{StaticResource ImageButtonStyle}" Grid.Column="2" Click="RemoveShowFromWatchlist">
                                <Image Source="../resources/remove_icon.png" Width="16" Height="16"/>
                            </Button>
                        </Grid>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
